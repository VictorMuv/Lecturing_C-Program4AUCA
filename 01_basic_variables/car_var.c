#include<stdio.h>

  void main(){

    char character= 'x';
   // here we are expeting to have the caracter as we have the c specifier
    printf("%c\n",character);
    

    // computers store characters as numbers (ASCII codes)
    printf("%d\n",character);



}
